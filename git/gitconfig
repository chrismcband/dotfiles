[user]
	name = Christopher McDonald
	email = chrismcband@gmail.com
[color]
	ui = auto
	interactive = auto
	branch = auto
	diff = auto
	status = auto
[github]
	user = chrismcband
[core]
	autocrlf = input
  excludesfile = /opt/boxen/config/git/gitignore
	editor = mate
[push]
	default = matching

[hub]
protocol = https

[credential]
helper = /opt/boxen/bin/boxen-git-credential
[filter "hawser"]
	clean = git hawser clean %f
	smudge = git hawser smudge %f
	required = true
